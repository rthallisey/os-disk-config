============
Installation
============

At the command line::

    $ pip install os-disk-config

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv os-disk-config
    $ pip install os-disk-config
